import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
     
    primary: {
        main: '#2F4F4F'
      },
      
    background: {
        default: "#FAF0E6",
        paper: 'white'
      }
  }
});

export default theme;